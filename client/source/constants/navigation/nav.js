import { PATHS } from '../app-paths/paths';


export const NAVPATHS = {
	Home: [
		{
			uri: PATHS.app.uri,
			label: 'Home'
		}
	],
	//
	// Dashboard module paths
	Dashboard: [
		{
			uri: PATHS.Dashboard.uri,
			label: 'Dashboard'
		}
	],

	//
	// Cooperative Tasks Board
	CooBoard: [
		{
			uri: PATHS.CooBoard.uri,
			label: 'CooBoard'
		}
	],

	Authentication: [
		{
			uri: PATHS.UserLogout.uri,
			label: 'Logout'
		}
	]
}

