import Auth from './auth';


const module = {
	name: 'CooBoard',
	component: Auth,
	mountPath: '/cooboard',
	api: [
		{
			id: 'addActivity',
			mountPath: '/'
		},
		{
			id: 'addActivity',
			mountPath: '/'
		}
	]
}

export

